# 源码统一 LF：本机 git 若开了 autocrlf，检出时会把 index.html 换成 CRLF，
# 而库内 _patch_*.py 一类脚本以文本模式重写文件，会把真实改动埋进整文件换行差异里。
*.html text eol=lf
*.js   text eol=lf
*.py   text eol=lf
*.css  text eol=lf
*.md   text eol=lf
*.txt  text eol=lf

# 素材强制二进制：PNG/GIF 被换行改写一个字节就废
*.png  binary
*.gif  binary
*.jpg  binary
*.jpeg binary
*.ico  binary
